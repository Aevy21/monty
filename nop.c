#include "monty.h"
/**
 *nop - does nothing
 *@stack: ponter to a stack
 *@line_number: an integer
 *Return: no return
 */
void nop(stack_t **stack, unsigned int line_number)
{
	(void) line_number;
	(void) stack;
}
